# Lean deploy set — exactly what the hosted Streamlit demo needs. The deployed
# pages (Dashboard + Trade Log) are SQLite-only, so this stays free of the heavy
# ML/RAG/broker stack. That's what keeps the Streamlit Community Cloud build fast
# and avoids compiling torch/tokenizers (which fail on Cloud's newer Python).
#
# For full local development (data pipeline, RAG index, LLM vetting, tests):
#     pip install -r requirements-full.txt
streamlit>=1.40
altair>=5.0
pandas>=2.0
pyyaml>=6.0
python-dotenv>=0.21
