# Full local development set: the data pipeline, RAG index, LLM vetting, paper
# broker, and the test suite. Install this to run everything locally:
#     pip install -r requirements-full.txt
# It includes the lean deploy set (streamlit/altair/pandas/pyyaml/dotenv) via:
-r requirements.txt

# --- data + broker + LLM --------------------------------------------------
yfinance>=0.2.40
alpaca-py>=0.43
anthropic>=0.69
pytest>=8.0

# --- RAG stack ------------------------------------------------------------
# Pinned to versions compatible with torch 2.2.x (the anaconda base ships
# torch 2.2.2). Newer sentence-transformers/transformers require torch>=2.4 and
# a huggingface_hub that dropped the HfFolder symbol they still import. Bump
# these together only alongside a torch>=2.4 upgrade. (These pins are why the
# heavy stack stays OUT of the Cloud deploy — see requirements.txt.)
chromadb>=0.5
sentence-transformers==2.7.0
transformers==4.41.2
huggingface_hub==0.23.4
tokenizers>=0.19,<0.20
