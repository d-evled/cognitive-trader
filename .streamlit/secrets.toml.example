# Streamlit secrets — copy to .streamlit/secrets.toml LOCALLY (gitignored), or
# paste into the Streamlit Community Cloud "Secrets" UI for a deployed app.
# Only needed to enable the Chat page's Claude-generated answers; retrieval and
# the Dashboard/Trade Log work without it.
ANTHROPIC_API_KEY = "sk-ant-..."
